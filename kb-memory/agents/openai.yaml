interface:
  display_name: "KB Memory"
  short_description: "Store durable reusable knowledge with the bundled kb-mcp client"
  default_prompt: "Use $kb-memory and its bundled client script to save durable reusable knowledge when it meets the write criteria."
